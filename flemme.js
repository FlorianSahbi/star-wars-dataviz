for (let index = 1; index < 89; index++) {
    console.log(`import character${index} from '../pic/character-${index}.jpg';`) 
}

for (let index = 0; index < 59; index++) {
    console.log(`import character999${index} from '../pic/character-999${index}.jpg';`) 
}